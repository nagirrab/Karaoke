# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.StaticController.index

GET     /app                        controllers.AppController.index

POST    /api/login                  controllers.api.UserController.login
GET     /api/logout                 controllers.api.UserController.logout

POST    /api/sessions               controllers.api.host.SessionController.create()
GET     /api/sessions/:id           controllers.api.host.SessionController.show(id: models.SessionId)
GET     /api/sessions               controllers.api.host.SessionController.list()
PUT     /api/sessions/:id           controllers.api.host.SessionController.update(id: models.SessionId)
POST    /api/sessions/:id           controllers.api.host.SessionController.update(id: models.SessionId)

# Map static resources from the /public folder to the /assets URL path
# GET     /assets/*file               controllers.Assets.at(path="/public", file)
GET     /webjars/*file              controllers.WebJarAssets.at(file)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file           controllers.Assets.versioned(path="/public", file: Asset)