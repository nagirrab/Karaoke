package services

import fixtures.DBSpecBase

class SessionServiceSpec extends DBSpecBase {
  "SessionService#reorderSession" when {
    "given an valid session" should {
      "order the items appropriately" in { s => pending }
    }
  }
}
